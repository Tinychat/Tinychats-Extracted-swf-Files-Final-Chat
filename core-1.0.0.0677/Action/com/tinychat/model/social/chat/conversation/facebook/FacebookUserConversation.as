package com.tinychat.model.social.chat.conversation.facebook
{
    import com.tinychat.model.api.*;
    import com.tinychat.model.social.chat.conversation.*;

    public class FacebookUserConversation extends AbstractUserConversation
    {

        public function FacebookUserConversation(param1:RenameableUser, param2:uint)
        {
            super(param1, param2);
            return;
        }// end function

    }
}
