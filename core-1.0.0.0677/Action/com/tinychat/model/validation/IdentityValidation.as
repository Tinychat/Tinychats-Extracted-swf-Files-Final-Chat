package com.tinychat.model.validation
{
    import com.tinychat.model.api.*;

    public interface IdentityValidation
    {

        public function IdentityValidation();

        function isValid(param1:Identity) : Boolean;

    }
}
